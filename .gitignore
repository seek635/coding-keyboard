# ---------- Python ----------
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.venv/
venv/
env/

# ---------- 编辑器 / 系统 ----------
.DS_Store
Thumbs.db
desktop.ini
*.swp
*.swo
.idea/
.vscode/
*.code-workspace

# ---------- 临时 / 备份 ----------
*.tmp
*.bak
*.orig
*.log
~$*

# ---------- 运行期状态 ----------
# 审批拨杆守护进程的状态目录（默认在用户目录，这里防误提交）
.claudepad/
*.token
token

# ---------- AI 工作记忆 ----------
# 记录的是协作过程（偏好、踩坑、内部推理），不是产品交付物。
# 如需作为「设计日志」公开，删掉下面这行即可。
.workbuddy-ai/

# ---------- 构建产物 ----------
# 技能打包件（由 skill-creator 生成，可重新打包，不入库）
dist/
