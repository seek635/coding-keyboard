# ClaudePad PCB 设计规格书

**版本**：v0.1
**日期**：2026-09-14
**板子尺寸**：98.00 × 50.00 × 1.60 mm（2026-09-16 从 86 加宽，解决 USB 口够不到右壁）
**配套文件**：`pcb/netlist.json` · `pcb/netlist.md` · `pcb/wiring.svg` · `cad/lib/params.scad`

> 本文件里的所有坐标均由 `tools/gen_pcb.py` 从 `cad/lib/params.scad` 生成，与外壳/定位板/固件严格对齐。

---

## 0. 快速导航

| 你想做的事 | 看哪节 |
|---|---|
| 手焊洞洞板 | §3 接线 + `pcb/wiring.svg` |
| 画 PCB | §4 封装坐标 + §5 元件布局 |
| **拿开源设计改板** | **`docs/PCB改板方案.md`** |
| 打样下单 | §8 制造参数 |
| 检查设计对不对 | §11 验证清单 |

---

## 1. 这块板子要做什么

把 11 个轴体 + 1 个审批拨杆 + 1 个主控 + 1 个电池座连起来，装进外壳。

**设计约束（全部来自已确定的结构与固件）**：

| 约束 | 来源 | 值 |
|---|---|---|
| 板子外形 | `params.scad` 的 `PCB_W` / `PCB_H` | 98 × 50 mm |
| 板厚 | `PCB_T` | 1.6 mm |
| 矩阵 | 固件 `claudepad.overlay` | 3 行 × 6 列 |
| 引脚 | 同上 | 行 D4/D5/D6，列 D7/D8/D9/D10/D16/D14 |
| 右侧缺口 | 拨杆本体在壳内的位置 | 深 8 × 长 24 mm |
| 轴体位置 | 定位板轴孔（`params.scad`） | 见 §4 |

---

## 2. 板子外形

```
        ┌──────────────────────────────────────────────┐
        │                                              │
        │             98.00 × 50.00                    │
        │                                              │┌──┐
        │              主矩阵区                         ││缺│
        │                                              ││口│ 8×24
        │                                              │└──┘
        └──────────────────────────────────────────────┘
```

**右侧缺口**：深 8mm、纵向 24mm，位置对准拨杆。
原因：SPDT 拨杆的**本体在壳内**（拨杆+轴套+螺母在壳外），本体沿轴套方向侵入约 8.5mm。
不给它让位，本体就会压到 PCB 上。

**四角不要放安装孔**：PCB 不靠螺丝固定——它由 14 个轴体「挂」着（轴体卡在定位板上，定位板用 4 颗 M2 螺丝锁在底座上）。
这是键盘的常规做法，PCB 无需额外固定。

---

## 3. 矩阵接线

完整接线图见 **`pcb/wiring.svg`**（可打印放大），焊接对照表见 **`pcb/netlist.md`**。

### 3.1 二极管方向（关键）

```
固件配置：diode-direction = "col2row"
含义：    二极管阳极接列线，阴极接行线
电流方向：列 → 行
```

每个按键回路：

```
列线 ──▶ 二极管阳极 ──▶ 二极管阴极 ──▶ 轴体 ──▶ 行线
```

**为什么是这个方向**：`col2row` 时列是**输出驱动**（`GPIO_ACTIVE_HIGH`），行是**输入读取**（`GPIO_ACTIVE_HIGH | GPIO_PULL_DOWN`）。二极管必须允许电流从被驱动的列流向被读取的行。

> ⚠️ **接反不会烧，但矩阵完全读不出来。**
> 修正二选一：把所有二极管掉头，或把固件里的 `diode-direction` 改成 `"row2col"`。
> 后者更省事，但以后别人看板子会困惑——建议前者。

### 3.2 引脚分配

| 信号 | 引脚 | 方向 |
|---|---|---|
| ROW1 / ROW2 / ROW3 | D4 / D5 / D6 | 输入（PULL_DOWN） |
| COL1 … COL6 | D7 / D8 / D9 / D10 / D16 / D14 | 输出 |
| RGB 数据 | D15 | 输出 |

> 这套分配必须与 `firmware/boards/shields/claudepad/claudepad.overlay` 一致。
> 改了这边就要改那边，`tools/check_keymap.py` 会检查键位顺序但**不检查引脚**，需人工确认。

### 3.3 大键并联（重要）

回车和空格在定位板上分别开了 2 个和 3 个轴孔（解决长键翘曲）。这些轴体在**电气上并联**：

| 大键 | 轴体数 | 都接到 |
|---|---|---|
| **回车**（2u） | 2 | ROW2 × COL4 |
| **空格**（4u） | 3 | ROW3 × COL1 |

**PCB 上要把这几个轴体的焊盘用铜箔连到同一条行/列线上**，否则会变成多个独立按键。
手焊洞洞板时，就是用一根导线把它们的引脚串起来。

### 3.4 审批拨杆

| 项 | 值 |
|---|---|
| 位置 | 右壁（不在轴体网格里） |
| 接线 | 公共端 → ROW1；常开端 → COL6（D14） |
| 二极管 | **建议串一只 1N4148** —— 拨杆是开关不是轴体，不串可能在多键同按时产生鬼影 |

拨杆是**两态**器件：拨到 ON 时持续接通 ROW1–COL6，固件读到的就是一个「一直按住的 F13」。

---

## 4. 轴体封装坐标

**Kailh Choc V2 (PG1353)**，原点 = 轴芯中心。

> 这组数据**已交叉验证两个独立开源封装库**（`daprice/keyswitches.pretty` 与 `siderakb/key-switches.pretty`），两者完全一致。

| 特征 | 坐标 (x, y) mm | 孔径 mm | 类型 |
|---|---|---|---|
| **电气引脚 1** | **(0, 5.9)** | 1.27 | 金属化（PTH） |
| **电气引脚 2** | **(-5, 3.8)** | 1.27 | 金属化（PTH） |
| 轴芯中心孔 | (0, 0) | **5.0** | 非金属化（NPTH）★ V2 加大（V1 是 3.429）|
| 侧固定孔 ×2 | (±5.5, 0) | 1.7018 | 非金属化（NPTH）★ V2 已取消 fixing pins，可省略 |

两电气引脚间距 = √(5² + 2.1²) = **5.42 mm**

**每个轴体的绝对位置**（PCB 坐标系，原点 = PCB 左下角）：

| 按键 | 轴体坐标 (x, y) |
|---|---|
| 访问等级 PERM | (7, 7) |
| ↑ UP | (25, 7) |
| 模型 MODEL | (43, 7) |
| 命令行 CMD | (61, 7) |
| 停止 STOP | (79, 7) |
| ← LEFT | (7, 25) |
| ↓ DOWN | (25, 25) |
| → RIGHT | (43, 25) |
| 回车 ENTER | (61, 25) **和** (79, 25) |
| 空格 SPACE | (7, 43) **(34, 43)** **(61, 43)** |
| 语音 VOICE | (79, 43) |

> 空格三个轴在 x = 7 / 34 / 61，跨 54mm，配合 4u 键帽的 3 个榫槽。

---

## 5. 元件布局

| 元件 | 位置 | 说明 |
|---|---|---|
| 14 个轴体 | 见 §4 | 主矩阵 |
| 14 只 1N4148 | 每个轴体旁 | 建议放在轴体与行线之间，占用小 |
| nice!nano v2 | 右上区域，**直接焊在板上** | 见下 |
| 审批拨杆引线焊盘 | 右侧缺口附近 | 公共端→ROW1，常开端→COL6 |
| 电池座（JST 1.25） | 左侧空区 | 电池在 PCB 正下方，走线穿过 |
| 电源开关焊盘 | 左侧 | 串在电池正极与主控之间 |
| 复位按钮焊盘 | 主控旁 | 并联到 RST |
| WS2812 | 沿轴体分布 | 数据线 D15 |

### 5.1 主控必须直焊，不能用排母

外壳内腔在 PCB 上方只有 **5.0mm** 净空（= `SWITCH_PLATE_GAP`），而 nice!nano 加上普通排母的高度约 8.5mm，**装不下**。

所以主控必须**直接焊接在 PCB 上**（板对板），或用超矮排针。这是「薄」换来的代价。

主控位置：PCB 右上区域，USB-C 口朝**右侧**，对准外壳右壁的 USB 开孔。

### 5.2 PCB 支撑台位置（与外壳的配合）

外壳里有 4 个矮柱托住 PCB，位置在 **PCB 坐标 (16,16)、(70,16)、(16,34)、(70,34)**。

> ⚠️ 这些位置是**轴体之间的空隙中心**，不是四角。
> 轴体之间只有 `18 − 13.5 = 4.5mm` 空隙，四角正好是轴体塑料脚的位置，支撑台放那里会顶住装不进去。
> 支撑台直径 4.0mm < 4.5mm 空隙。

画 PCB 时**不需要**为它们开孔（支撑台在 PCB 下方，只是托着）。

---

## 6. 布线规格

| 项 | 建议值 | 理由 |
|---|---|---|
| 铜厚 | 1 oz（35µm） | 常规，够用 |
| 行/列信号线宽 | **0.25 mm** | 信号线电流极小 |
| 电源线宽（3.3V / VBAT） | **0.6 mm** | 主控峰值电流 + RGB |
| GND 线宽 | **0.6 mm** 或铺铜 | 回流路径 |
| 线间距 | ≥ 0.2 mm | 嘉立创常规工艺 |
| 过孔 | 0.3/0.6 mm | 常规 |
| 层数 | **2 层** | 足够，成本低 |

**布线要点**：

1. **行线/列线尽量走到底层**，顶层留给轴体焊盘和元件
2. **WS2812 的数据线串一颗 100–300Ω 电阻**（靠主控端），防振铃
3. **RGB 电源加 100µF 电解 + 0.1µF 陶瓷**去耦，否则灯珠闪烁
4. 天线区（nice!nano 的板载天线下方）**不要铺铜、不要走线**，否则蓝牙距离骤降
5. 大键并联的几个轴体，用同一根粗一点的线（0.4mm）串起来

---

## 7. 与外壳/定位板的配合

| 接口 | 值 | 校验方式 |
|---|---|---|
| 板子外形 | 98 × 50 mm | `check_dims.py` 第 7 项 |
| 板厚 | 1.6 mm | 与 `PCB_T` 一致 |
| 轴体位置 | 与定位板轴孔同心 | `gen_pcb.py` 从 `params.scad` 取 |
| 右侧缺口 | 深 8 × 长 24 mm | `check_dims.py` 第 11 项 |
| PCB 上方净空 | 5.0 mm | 决定主控必须直焊 |
| PCB 下方净空 | 5.0 mm | 电池 + 走线 |
| 支撑台避让 | 支撑台在空隙中心 | `check_dims.py` 第 19–22 项 |

**装配关系**：

```
        定位板（4 颗 M2 螺丝锁在底座上）
          │
          │ 轴体穿过定位板、焊在 PCB 上
          ▼
        PCB  ──── 由轴体「挂着」，同时被 4 个支撑台托住
          │
          │ 电池 + 走线在 PCB 下方
          ▼
        底座底板
```

---

## 8. 制造参数（嘉立创打样）

| 参数 | 值 |
|---|---|
| 层数 | 2 层 |
| 尺寸 | 98 × 50 mm |
| 板厚 | 1.6 mm |
| 铜厚 | 1 oz |
| 阻焊颜色 | 任意（建议哑黑，视觉干净） |
| 丝印 | 白 |
| 表面处理 | 有铅喷锡（最便宜）或沉金 |
| 最小线宽/间距 | 0.25 / 0.2 mm（常规工艺范围内） |
| 最小孔径 | 0.3 mm（本项目最小是 1.27，很宽裕） |

> 板子很小，嘉立创 5 片起打，成本极低。建议**多打几片**——首片焊坏了不用重等。

---

## 9. 打板前必做的三件事

1. **先打外壳和定位板，实测 `SWITCH_PLATE_GAP`**
   （用 `cad/test_coupon.scad` 校准件，15 分钟）
   这个值决定 PCB 上轴体焊盘与定位板的相对高度关系，虽然 PCB 本身不受影响，
   但**主控高度**受它约束——如果实测值 < 3.7mm，主控方案要改。

2. **确认拨杆实物尺寸**
   轴套直径/长度、面板尺寸、**本体侵入深度**。本体侵入 > 8.5mm 的话，缺口要加深。

3. **确认 nice!nano 的封装尺寸**
   17.8 × 33mm 是标称值，实物可能有偏差。特别是 USB-C 口的位置——它要和外壳右壁的开孔对齐。

---

## 10. 手焊洞洞板（不用打 PCB 的路线）

如果想先验证电路再打板，按 `pcb/wiring.svg` 在洞洞板上焊：

**需要的洞洞板**：至少 90 × 54 mm（留余量）

**步骤**：

1. 先焊 14 只二极管，**统一方向**（阳极朝列）
2. 用裸铜线沿列走 6 条、沿行走 3 条，构成矩阵
3. 每个轴体位置焊两个排针座（或用飞线直接连轴体）
4. 大键的多个轴体，用一根线把它们的引脚串起来
5. 最后接主控、拨杆、电池座

> 洞洞板版本的轴体无法插进去固定，只能**用飞线把轴体引出来**，或者先不装轴体、用镊子短接焊盘测试固件。

---

## 11. 验证清单

打板前逐项确认：

```
□ 板子外形 98 × 50 mm
□ 右侧缺口 深 8 × 长 24 mm，位置对准拨杆
□ 14 个轴体位置与 §4 表格一致
□ 轴体焊盘坐标 (0,5.9) 与 (-5,3.8)，孔径 1.27
□ 中心孔 **5.0** 非金属化（V2）、侧孔 1.7018 非金属化（V2 可省略）
□ 二极管统一方向：阳极接列、阴极接行
□ 回车两个轴、空格三个轴各自并联到同一交点
□ 引脚分配与 claudepad.overlay 一致
□ 主控天线区无铺铜、无走线
□ 主控 USB 口位置与外壳右壁开孔对齐
□ PCB 四角无安装孔（不需要）
□ 支撑台位置不需开孔
```

---

## 12. 本规格书的边界

**包含**：板子外形、矩阵接线、封装坐标、元件布局、布线规格、与外壳的配合。

**不包含**：
- ❌ **具体 PCB 布局文件**（KiCad 工程）——本文件给的是**设计规格与坐标**，画板需要你在 KiCad 里落地
- ❌ **原理图符号库**——直接用 KiCad 自带的 Switch/Diode 符号即可
- ❌ **RGB 灯珠的具体数量与排布**——取决于你想要的灯效，见 PRD 第 8 章

**未验证项**：
- 本规格书的数据来自 `params.scad` 与两个开源封装库，**逻辑与坐标已交叉验证**
- 但**没有生成实际的 PCB 文件并用 DRC 检查过**，也没有打样实测
- 首次画板后，请用 KiCad 的 DRC 跑一遍，并对照 §11 清单人工确认

---

## 13. 能买现成的 PCB 吗？

**结论：不能。** 而且原因比"布局特殊"更根本。

### 13.1 根本障碍：我们的轴体不在标准网格上

标准宏键盘 PCB 是**均匀网格**——每 18mm（Choc）或 19.05mm（MX）一个格点，每个格点一个独立矩阵节点。

本设计的轴体坐标（PCB 原点在左下，单位 mm）：

| 键 | 坐标 |
|---|---|
| 第 1 行 5 键 | x = 7, 25, 43, 61, 79（y=7） |
| 第 2 行 3 键 + 回车 2 轴 | x = 7, 25, 43, 61, 79（y=25） |
| 空格 3 轴 | **x = 7, 34, 61**（y=43） |
| 语音 | x = 79（y=43） |

网格线是 **7 / 25 / 43 / 61 / 79**。而空格的中间那根轴在 **x = 34** —— **它不在网格上**。

这不是失误：4u 键跨列 1–4，跨度 72mm，三点对称支撑落在 1/6、3/6、5/6 处，中点正好是跨度中心。
**任何均匀网格的 PCB 都放不下它。**

### 13.2 其余三个不匹配

| 维度 | 本设计 | 市面现成宏键盘 PCB |
|---|---|---|
| 主控 | nice!nano（nRF52840）+ BLE | 多为 Pro Micro（ATmega32u4）+ USB 有线 |
| 固件 | ZMK | 多为 QMK（引脚与配置不通用） |
| 轴体 | Kailh Choc 矮轴，18mm 键距 | 多为 MX，19.05mm 键距 |
| 板形 | 98 × 50，右侧 8×24 拨杆缺口 | 尺寸与缺口都对不上 |

### 13.3 那怎么办？三条实际路径

| 路径 | 说明 | 适用时机 |
|---|---|---|
| **A. 洞洞板手焊** | 用 §10 的接线图 + `pcb/wiring.svg` 直接焊。零等待、成本最低 | **原型验证阶段（推荐先走这条）** |
| **B. 拿开源设计改** | 立创开源硬件平台（oshwhub）上有现成的**无线矮轴小键盘**项目，已解决最难的部分：nice!nano 引脚、电池接口、充电电路、电源开关、Choc 轴座封装、ZMK/RMK 固件。只需改布局 + 加拨杆缺口。**详细方案见 `docs/PCB改板方案.md`** | 想要正式 PCB 但不想从零画 |
| **C. 从零画板打样** | 本规格书已给全部网表与坐标，2 层板，嘉立创 5 片起 | 布局定稿后 |

**参考开源项目**（oshwhub 搜索可得，均为无线矮轴 + nice!nano）：
- **EPPad** —— 无线小键盘，凯华/佳达隆两种轴座孔，资料最全（原理图+PCB+BOM+结构）→ **推荐作底板**
- **TK13** —— 凯华矮轴，结构简单，但无 BOM

> ⚠️ 这些项目**布局都与本设计不同**，只能拿来复用「主控电路 + 轴座封装 + 固件框架」，
> 布局必须自己改。**具体怎么做见 `docs/PCB改板方案.md`** —— 含复用清单、
> 三个必先解决的冲突（热插拔高度冲突 / 板形 / 离格轴）、分步流程与验证清单。

### 13.4 为什么建议先洞洞板

1. **布局还没经过实物验证** —— 大键手感、拨杆位置、整体厚度都只是纸面设计
2. **洞洞板改起来零成本**，PCB 改一次要等一周
3. 项目本来就是分步推进的（`docs/采购清单.md` 第 1 步 → 第 3 步）

**顺序建议**：校准试件 → 洞洞板原型 → 实机验证手感 → 定稿布局 → 打样 PCB。

---

## 14. ★ 已发现的阻塞问题：主控放不下

**2026-09-15 发现。** 这一条影响**所有**路径（买现成 / 改开源 / 自己画）——
不是 PCB 的问题，是**布局本身没有给主控留位置**。

### 14.1 问题

主控 nice!nano 是 **17.8 × 33.0 mm**，而板面只有 **86 × 50 mm**，
上面已经摆了 **14 个轴体**（本体 13.5mm 见方，占 2550 mm²，板面共 4300 mm²）。

穷举板面所有位置（步长 0.5mm）：

| 检查 | 结果 |
|---|---|
| 当前设计位置（PCB 坐标 65.2, 14.0） | ❌ 与轴体重叠 **322.52 mm²** |
| 板面上零重叠的位置数 | **0 个** |

重叠的 4 个轴体：

| 键 | 轴心 | 重叠面积 |
|---|---|---|
| 回车（第 2 轴） | (79, 25) | ~146 mm²（几乎整个轴体） |
| 语音 | (79, 43) | ~116 mm²（几乎整个轴体） |
| 回车（第 1 轴） | (61, 25) | ~34 mm² |
| 空格（第 3 轴） | (61, 43) | ~27 mm² |

**根因**：3×5 网格在 18mm 键距下需要 **90 × 54 mm** 才排得开，
而 PCB 只有 86 × 50 —— 轴体本体几乎贴到板边（四边各余 0.25mm），**没有任何连续空位**。

> 这条已固化为 `tools/check_dims.py` 第 11 节的两项自动检查，不会再被漏掉。

### 14.2 解决方案（推荐：主控移到 PCB 背面）

PCB 下方有 **5.0 mm** 层高，主控厚 **3.2 mm** —— **放得下**。

背面布局（PCB 坐标）：

```
        ┌────────────────────────────────────────┐
        │                                        │
        │        电池 30×40（居中偏左）           │
        │      x 30.55~60.55  y 7.55~47.55       │
        │                          ┌───────────┐ │
        │                          │  主控      │ │
        │                          │ 17.8×33   │ │
        │                          │x68~85.8   │ │
        │                          │y8.5~41.5  │ │
        └──────────────────────────┴───────────┴─┘
```

**推荐位置**：`x = 68.0, y = 8.5`（最靠右，USB-C 朝右壁）

### 14.3 连带要改的三处

| # | 改什么 | 从 | 到 |
|---|---|---|---|
| 1 | **右侧支撑台** | x = 70（会被主控压住） | **x = 52**（合法的轴体间隙中心） |
| 2 | **USB-C 开孔高度** | PCB 上表面 +1.8mm | PCB 下表面 −1.8mm |
| 3 | 天线净空 | 主控正上方禁铺铜 | 改为**主控正下方**（PCB 背面）禁铺铜 |

**支撑台为什么能挪**：合法位置是轴体间隙中心，x ∈ {16, 34, 52, 70}、y ∈ {16, 34}。
把右侧两个从 70 挪到 52 仍在合法集合内，且避开主控（52 < 68）。

### 14.4 备选方案

| 方案 | 做法 | 代价 |
|---|---|---|
| **A. 主控放背面**（推荐） | 见 §14.2 | USB 开孔下移；天线区改到背面 |
| B. 换更小主控 | 用 E73-2G4M08S1E 等模块（约 13×18mm） | 需自己加充放电电路；固件要改引脚 |
| C. 加大板子/外壳 | PCB 加宽到 96mm 以上 | 整机尺寸变大，外壳全部重做 |
| ~~D. 改用 ESP32~~ | **已评估，否决**（见 §14.4.1） | — |

**方案 B 的额外好处**：EPPad 作者也提到 nice!nano 多为 2020 年产芯片、**容易坏**，
建议改用 E73 + 自加充放电。如果要走这条路，值得一并考虑。

### 14.4.1 已评估并否决：ESP32（2026-09-16）

用户问过「能不能用 ESP32-WROOM-32 做主控」。**结论：不行，三个硬伤。**

| 硬伤 | 数据 | 来源 |
|---|---|---|
| **① 装不进** | ESP32-DevKitC 开发板 **54.4 × 27.9 × 10 mm**；而键盘 PCB 上方净空 **3.40mm**、下方 **5.0mm** | 乐鑫官方 / 厂商规格 |
| **② 焊不了** | 纯模组 **18.00 × 25.50 × 3.10 mm**，38 脚，**引脚间距 1.27mm**。洞洞板是 2.54mm 网格，插不进；手工焊需热风枪或拖焊 | `ESP32-WROOM-32_datasheet_cn.pdf` v3.8 第 32 页 |
| **③ 固件要自己写** | ESP32 **跑不了 ZMK**，需用 ESP-IDF + FreeRTOS 自己实现矩阵扫描 / 去抖 / BLE HID 报告 / 休眠状态机 | — |

**外加：功耗差一个数量级**

| 方案 | 500mAh 电池续航 | 出处 |
|---|---|---|
| nice!nano (nRF52840) | **约 25 天** | kriscables 官方 FAQ（110mAh + 每天 8h = 5–7 天，换算） |
| ESP32-C3 低功耗模式 | 约 2 天 | 实测（9.5mA） |
| ESP32-C3 默认设置 | 约 5 小时 | 实测（93mA） |
| ESP32-WROOM-32 | 比 C3 更差（初代双核，非低功耗设计） | — |

> **关键差异不在睡眠电流，而在连接状态**：nRF52840 深度睡眠 **20µA 且保持蓝牙连接**；
> ESP32 深度睡眠虽低至 5µA，但**必须断开蓝牙**，唤醒后需 **3–5 秒**重连，期间按键无响应。
> 对键盘而言这是体验硬伤。

**外围也全要自己加**：无 USB（需 CH340/CP2102）、无稳压（锂电 3.7–4.2V 需 LDO）、
无充电管理（需 TP4056/IP5306）。nice!nano 全部板载。

**如果确实想走 ESP32 系**：选 **ESP32-C3** 而非 WROOM-32（功耗与尺寸都更好，
且内置 USB Serial/JTAG 可直接烧录，省掉一个芯片）。立创开源平台有现成的
「ESP32-C3 17 键 BLE 数字小键盘」可参考（CC BY-NC-SA 4.0，Gitee 有源码）。

**如果想省钱**：用 **SuperMini nRF52840**（约 20–30 元）—— Pro Micro 引脚兼容、
**2.54mm 通孔**、能跑 ZMK、低功耗。该有的都有，钱也省了。

### 14.5 待办

- [x] 确认采用哪个方案 —— **2026-09-16 选定方案 A（主控放背面）**
- [x] 更新 `cad/lib/params.scad`（加主控位置参数）
- [x] 更新 `cad/case_bottom.scad`（挪支撑台、移 USB 开孔、电池让位）
- [x] 重新跑 `tools/check_dims.py` 确认 26 项全绿
- [x] 重新导出 STL 并验证

**实施记录（2026-09-16）**

| 项 | 从 | 到 |
|---|---|---|
| 主控安装面 | PCB 正面（上表面） | **PCB 背面（下表面）**，`NANO_ON_BACK = true` |
| 主控位置（PCB 坐标） | (65.2, 14.0) | **(68.0, 8.5)** |
| 支撑台 x | 16 / 70 | **34 / 52** |
| 电池 x（PCB 坐标） | 28.15 ~ 58.15（居中） | **−8 ~ 22（贴内腔左壁）** |
| USB 开孔 z | `PCB_TOP_Z + 1.80` = 10.40 | **`PCB_BOTTOM_Z − 1.80` = 5.20** |

**为什么电池也要挪**：主控移到背面右侧后，支撑台必须用 x=34/52 这两个间隙中心
（x=70 被主控占、x=16 让给电池）。而**居中的电池（x 28.15~58.15）正好盖住 34 和 52** ——
四个候选位置会全部冲突。所以电池让位到左侧，PCB 坐标 x −8~22。

> 这是改板过程中**新暴露**的连锁问题：原设计（主控在正面、电池居中、支撑台 16/70）
> 本身是自洽的，但一旦主控移到底面右侧，三个元件就在同一层里抢位置。

**校验结果**：`check_dims.py` **26 项全部通过**（项目首次全绿）。
`lint_scad` / `verify_geometry` / `check_keymap` / `verify_stl` 全部通过，6 个 STL 已重新导出。

**⚠️ 已打印的外壳需要重做**：支撑台位置在底板上、USB 开孔在右壁上，两处都变了。
顶盖和定位板不受影响。

---

### 14.6 ✅ 已解决：USB 口够不到外壳右壁（2026-09-16 发现并当天解决）

**现象**：主控装在 PCB 背面后，USB 口位于主控右缘 = PCB 坐标 x 85.8。
PCB 右边缘在 86，而内腔右壁在 94 —— **USB 口距内腔右壁 8.2mm**。

**后果**：标准 USB-C 插头的金属部分只有 **6.5mm**，插入时够不到 USB 座，
**插不到底、可能接触不良**。

**根因**：PCB 比内腔小 16mm（每边 8mm）。
`PCB_W = GRID_W − 4 = 86`，而定位板 `= GRID_W + 12 = 102`，内腔 102.3。
**无论主控放正面还是背面、横放还是竖放，这个距离都消不掉** ——
换安装面只是把它从 11.3mm 改善到 8.2mm，没有根治。

**四个候选解法**：

| 方案 | 做法 | 代价 |
|---|---|---|
| **A. USB-C 面板延长线**（推荐） | 买一根「面板安装式 USB-C 公转母延长线」，公头插主控、母座固定在右壁孔上 | 多一个元件（约 10 元）；右壁开孔改为按母座尺寸 |
| B. PCB 加宽 | `PCB_W` 从 86 加到约 100，四角切角避开螺丝柱 | 改 PCB 设计；需重新验证轴体焊盘与定位板对位 |
| C. 主控 USB 端探出 PCB | 主控右移，右侧引脚用飞线补 | 引脚悬空，不可靠，不推荐 |
| D. 接受现状 | 插头只能插到 6.5mm | **不推荐**，可能接触不良 |

**状态**：✅ **已解决 —— 采用方案 B（PCB 加宽）**

用户 2026-09-16 决定「不买延长线，直接改 PCB」（外壳尚未开工，改动成本最低）。

**实施**：`PCB_W` 从 86 加宽到 **98**（`PCB_X` 由 8 变 2），主控随之右移到 x=80。

| 检查 | 结果 |
|---|---|
| PCB 右缘距内腔右壁 | **2.15 mm** |
| 插头插入深度 = 壁厚 + 该距离 | 2.4 + 2.15 = **4.55 mm** |
| 插头金属部分长度 | 6.5 mm |
| 判定 | ✅ **4.55 < 6.5，够得到** |

**为什么加宽不会撞四角螺丝柱**：PCB 占 y 8~58，而螺丝柱占 y 1.5~6.5 与 59.5~64.5 ——
两者在 y 方向天然错开，所以 PCB 在 x 方向可以一直延伸到接近内腔壁。

**连带改动**：PCB 加宽使 `PCB_X` 从 8 变 2，所有「列间隙中心」整体左移 6mm，
支撑台从 34/52 改为 **40/76**（间距 36mm，对称，且与电池、主控都留有余量）。

**副产物**：PCB 面积从 4300 增至 4900 mm²，布线余量更大。

**同时发现并修复的检查器 bug**：`check_dims.py` 里 x 和 y 方向的偏移**都用了 PCB_X**。
原设计 `PCB_X == PCB_Y == 8`，巧合正确；PCB 加宽后 `PCB_X` 变 2，y 方向检查立即误报。
已改为各自使用 `PCB_X` / `PCB_Y`。

---

## 附：一页速查

```
【板子】86 × 50 × 1.6 mm，2 层，右侧缺口 8×24

【矩阵】3 行 × 6 列
  行 D4/D5/D6（输入，PULL_DOWN）
  列 D7/D8/D9/D10/D16/D14（输出）

【二极管】col2row：阳极接列、阴极接行，电流 列→行

【轴体封装】Choc V1，原点=轴芯中心
  电气脚1 (0, 5.9)  电气脚2 (-5, 3.8)   孔径 1.27
  中心孔 (0,0) 3.429 NPTH ｜ 侧孔 (±5.5,0) 1.7018 NPTH

【大键并联】回车 2 轴 → ROW2×COL4 ｜ 空格 3 轴 → ROW3×COL1

  【主控】必须直焊，不能用排母（上方只有 3.40mm 净空，见 §14）
  【主控选谁】nRF52840 系（nice!nano / SuperMini / E73）。不要 ESP32（见 §14.4.1）
  【支撑台】外壳侧在 PCB (34,16)(52,16)(34,34)(52,34)，板子上不用开孔

【生成命令】python tools/gen_pcb.py
【改键位后】python tools/check_keymap.py 会校验固件与结构是否一致

【能买现成的吗】不能 —— 空格中间轴在 x=34，不在 18mm 网格上（见 §13）
【推荐顺序】校准试件 → 洞洞板原型 → 验证手感 → 定稿 → 打样

  【主控】★ 已定案（2026-09-16）：装在 PCB 背面（方案 A）
    位置 x=68, y=8.5（PCB 坐标），USB-C 朝右壁
    → 支撑台 16/70 改 34/52 ｜ 电池贴左壁 ｜ USB 开孔 z 10.40 → 5.20
    ⚠️ 遗留：USB 口距内腔右壁 8.2mm，插头够不到（见 §14.6）
```
